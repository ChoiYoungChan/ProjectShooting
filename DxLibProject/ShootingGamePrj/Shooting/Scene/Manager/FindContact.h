#pragma once
#include "DxLib.h"
#include <math.h>

namespace contact
{
	class FindContact
	{
	public:
		FindContact() = default;
		~FindContact() = default;

		inline bool ConfirmContact(int, int)
		{
			int distance = sqrt(pow(,2));

		}

	private:

	};
}